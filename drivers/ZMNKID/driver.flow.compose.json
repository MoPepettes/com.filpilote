{
  "triggers": [
    {
      "$extends": "offAutoThermostatModeChanged"
    }
  ],
  "actions": [
    {
      "$extends": "resetMeter"
    },
    {
      "$extends": "setOffAutoThermostatMode"
    }
  ]
}